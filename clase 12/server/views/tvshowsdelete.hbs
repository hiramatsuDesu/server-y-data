<div class="container">
    <div class="card shadow-lg">
        <div class="card-header">
            <h1>Eliminar Película...</h1>
            <h3>Está seguro que desea eliminar esta película?</h3>
        </div>
        <div class="card-body">
            <form id="tvshowdelete" action="/shows?_method=DELETE" method="POST" enctype="application/json">
                <input type="hidden" name="_id" value="{{tvshow._id}}">
                
                <div class="form-group">
                    <input class="form-control" type="text" name="title" placeholder="Título de la película..." readonly value="{{tvshow.title}}">
                </div>
                <div class="mt-3">
                    <button class="btn btn-success btn-lg" type="submit">Si, eliminar</button>
                    <a class="btn btn-danger btn-lg" href="/shows">NO!, volver</a>
                </div>
            </form>
        </div>
    </div>
</div>